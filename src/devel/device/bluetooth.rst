=========
Bluetooth
=========

By default bluetooth is not configured to work. But the module exists, the kernel is built to use it.

Enabling bluetooth
------------------

.. code-block:: shell

  modprobe btnxpuart
  bluetoothctl show
